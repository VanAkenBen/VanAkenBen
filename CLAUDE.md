# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project Overview

This is a GitHub profile README repository. The `README.md` at the root is displayed on the GitHub profile page at github.com/VanAkenBen.

## Important

- The repo must be named **VanAkenBen** (matching the GitHub username exactly) for the profile README to appear.
- The Xcode project (`BenVanAken.xcodeproj`) is a leftover from initial setup and can be removed.
